/var/vcap/sys/log/docker/*.log {
  missingok
  rotate <%= p('daemon.logrotate.rotate') %>
  compress
  delaycompress
  notifempty
  copytruncate
  size <%= p('daemon.logrotate.size') %>
  su <%= p('user') %> <%= p('group') %>
}
